import FormLogin from "../form/FormLogin";

function LoginPage(){
    return (
        <div className="App">
            <header className="App-header">
                <FormLogin />
            </header>
        </div>
    );
}

export default LoginPage